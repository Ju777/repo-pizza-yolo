qtyToUpdate = document.getElementById("qty-item-id-<%=@cart_product.id%>");

if (qtyToUpdate) {
  qtyToUpdate = qtyToUpdate.innerHTML
  qtyToUpdate = parseInt(qtyToUpdate)
  document.getElementById("qty-item-id-<%=@cart_product.id%>").innerHTML = qtyToUpdate + 1

  // coder la mise à jour du TOTAL A PAYER ici
  productPrice = parseFloat("<%=@cart_product.product.price%>");

  cartTotal = document.getElementById("cart-total").innerHTML;
  cartTotal = parseFloat(cartTotal);

  document.getElementById("cart-total").innerHTML = cartTotal + productPrice;

};
